/* eslint-disable arrow-body-style */
const service = require('./outlet.service');

describe('Service - outlet', () => {
  beforeEach(() => { });

  afterEach(() => { });

  it('TODO: should do unit test for ', () => {
    service.getOutlet();
  });
});
