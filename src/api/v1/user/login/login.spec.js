/* eslint-disable arrow-body-style */

describe('Test login', () => {
  beforeEach(() => { jest.useFakeTimers(); });

  afterEach(() => { });

  it('TODO: should do unit test for login', () => {
  });
});
