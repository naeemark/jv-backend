/* eslint-disable arrow-body-style */

describe('Test get', () => {
  beforeEach(() => { jest.useFakeTimers(); });

  afterEach(() => { });

  it('TODO: should do unit test for get', () => {
  });
});
