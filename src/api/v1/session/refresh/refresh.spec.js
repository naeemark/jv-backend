/* eslint-disable arrow-body-style */

describe('Test refresh', () => {
  beforeEach(() => { jest.useFakeTimers(); });

  afterEach(() => { });

  it('TODO: should do unit test for refresh', () => {
  });
});
